Workflows orchestrate multi-step processes across agents using a DAG of nodes — including agent calls, conditional switches, loops, and map-reduce fan-outs. Use a workflow when the logic is deterministic and better expressed as structured control flow rather than left to an agent to reason through.

You can use AI-assisted tooling to generate YAML configurations for workflows by running `sam ai-assistance` or asking Claude Code with the SAM skills loaded.
