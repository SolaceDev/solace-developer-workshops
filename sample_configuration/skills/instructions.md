Skills are reusable, packaged bundles of agent instructions and assets that can be loaded into any agent at runtime. They allow you to share domain expertise, prompt libraries, and document templates across multiple agents and projects without duplication.

You can use AI-assisted tooling to generate YAML configurations for skills by running `sam ai-assistance` or asking Claude Code with the SAM skills loaded.
