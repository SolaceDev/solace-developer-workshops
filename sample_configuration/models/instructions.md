Models define the LLM provider configuration used by agents — including the provider, model ID, API credentials, and parameters like temperature and max tokens. Agents reference a model by name, making it easy to swap or tune the underlying LLM across environments.

You can use AI-assisted tooling to generate YAML configurations for models by running `sam ai-assistance` or asking Claude Code with the SAM skills loaded.
