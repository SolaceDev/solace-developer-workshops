Agents are the core AI workers in Solace Agent Mesh — each agent has a system prompt, a set of tools, and an optional structured input/output schema. They handle user requests, call tools, and can delegate to peer agents to collaborate on complex tasks.

You can use AI-assisted tooling to generate YAML configurations for agents by running `sam ai-assistance` or asking Claude Code with the SAM skills loaded.
