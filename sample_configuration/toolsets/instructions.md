Toolsets are custom Go or Python tool packages that extend what agents can do — each toolset is built from source and deployed to the platform, then attached to agents that need those capabilities. They are the right choice when no built-in connector covers your integration.

You can use AI-assisted tooling to generate YAML configurations for toolsets by running `sam ai-assistance` or asking Claude Code with the SAM skills loaded.
