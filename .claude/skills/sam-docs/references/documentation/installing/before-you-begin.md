---
title: Before You Begin
description: Procure a broker, choose an LLM provider, and confirm network reachability before installing Solace Agent Mesh — the universal prerequisites for every install path.
sidebar_position: 310
---

# Before You Begin

<!-- WRITER NOTE: Source: archive/getting-started/before-you-begin.md. Rewrite to Solace style. Do NOT copy/paste. -->

This page covers the prerequisites that apply before you install Agent Mesh. Read this page before following any of the installation guides.

## Event Broker

<!-- WRITER NOTE: Does not apply to the desktop bundle (development event broker included). Applies to all other options. Cover: Solace Cloud option, self-hosted PubSub+ option, minimum supported version, connection details you will need (host, port, credentials, Message VPN name). Include the sam doctor command if it can verify broker connectivity. -->

*Content forthcoming.*

## LLM Provider

<!-- WRITER NOTE: Applies to all installation options including desktop bundle. Cover: list of supported providers, the environment variable name for each provider's API key, how to verify the key is set correctly, and the network reachability requirement (outbound HTTPS to the provider endpoint). -->

*Content forthcoming.*

## System Requirements

<!-- WRITER NOTE: Cover per-option requirements in a table: OS, CPU architecture, RAM, disk. Rows = Desktop Bundle / CLI Binary / Docker / Kubernetes. -->

*Content forthcoming.*

## Network Requirements

<!-- WRITER NOTE: Cover: outbound HTTPS to LLM provider (all options); event broker connectivity (all options except desktop bundle); ingress requirements for Kubernetes; any firewall or proxy considerations. -->

*Content forthcoming.*
